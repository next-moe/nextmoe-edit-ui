# Keep the dependency tree strict so every package can only import what it
# declares. @nextmoe/edit-ui-core must stay framework-free: with a strict tree a
# stray `import ... from 'vue'` in core fails to resolve instead of silently
# picking up a hoisted copy from the vue package.
shamefully-hoist=false
